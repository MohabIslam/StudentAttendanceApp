@model StudentAttendanceApp.Models.Track
@* @{
    ViewData["Title"] = "Delete Track";
} *@

<div class="container mt-4">
    <div class="card shadow">
        <div class="card-header bg-danger text-white">
            <h3>🗑 Delete Track</h3>
        </div>
        <div class="card-body">
            <p class="text-danger fw-bold">⚠ Are you sure you want to delete this track?</p>
            <dl class="row">
                <dt class="col-sm-3">Track Name</dt>
                <dd class="col-sm-9">@Model.Name</dd>

                <dt class="col-sm-3">Branch</dt>
                <dd class="col-sm-9">@Model.Branch.Name</dd>
            </dl>
            <form asp-action="DeleteConfirmed" method="post">
                <input type="hidden" asp-for="Id" />
                <button type="submit" class="btn btn-danger">🗑 Confirm Delete</button>
                <a asp-action="Index" class="btn btn-secondary">⬅ Cancel</a>
            </form>
        </div>
    </div>
</div>
